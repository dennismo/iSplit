version: '3'
services:
    
  backend:
    build: 
      context: .
      args: 
        - APP_1_PORT
    ports:
      - "${APP_1_PORT}:${APP_1_PORT}"
    environment:
      - SERVICE_1_URL
      - SERVICE_2_URL
      - APP_1_CTX
      - APP_1_PORT
      - APP_2_CTX
      - APP_2_PORT

